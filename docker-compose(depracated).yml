services:
  api:
    build: .
    container_name: api-sumarizacao
    image: fastapi-simepar-teste
    ports:
      - "8000:8000"
    volumes:
      - ./dados_estacao.duckdb:/app/dados_estacao.duckdb


